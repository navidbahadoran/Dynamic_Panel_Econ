# RR2 decision

## RR2-PARTIAL

The generic selector is fully specified. The raw RR1 coefficient spectrum has been replaced by the
dimensionless fitted-value-scale spectrum

`(w_M/w_A,1)^2 sigma_j(M_hat^pil)^2/(NT)`,

which is algebraically unit-equivariant under coherent reparameterization and preserves the required signal
and noise rates under existing assumptions. The deterministic anchor remains coherent after this
normalization.

The generic pilot theorem, positive-rank proof, zero-rank proof, truth-at-cap proof conditional on the generic
pilot rate, unique blockwise minimum, finite-union joint theorem, and downstream inference transfer are complete.
The rectangular proof preserves the exact maintained growth condition. No substantive DGP assumption is
added.

The literal numerical box is correctly treated as a canonical-unit constraint: holding its numerical value
fixed while changing coefficient units would define a different constrained problem, not a failure of the
rank normalization.

One precise issue remains. Revision 9's `a:identification` controls only differences generated by candidates
inside the reporting caps. The cap+1 pilot permits difference rank `2bar_r_M+1` at cap truth, outside that
domain. Therefore the enlarged pilot's objective gap cannot by itself yield the generic operator rate. A
proof would require a strengthened identification domain, a high-level pilot-rate assumption, or a different
extra-spectrum construction.

Classification: **RR2-PARTIAL**.

Unresolved mathematical issue: **cap+1 pilot curvature/operator-rate corollary under unchanged assumptions**.
